import React from "react";

const Header = ({ onShowContent }) => {
  return (
    <header className="min-h-screen relative flex items-center justify-center bg-gradient-to-br from-black to-gray-900">
      <div className="text-center z-10 p-8">
        <h1 className="text-6xl font-bold mb-6 relative">
          <span className="inline-block transform hover:scale-105 transition-transform text-white shadow-[0_0_2px_#00ff00,0_0_4px_#00ff00,0_0_6px_#00ff00]">
            R MADHAV SAPKOTA
          </span>
        </h1>
        <p className="text-2xl mb-8 text-red-500 uppercase tracking-wider">
          Cybersecurity Engineer & Red Team Specialist
        </p>
        <button
          onClick={onShowContent}
          className="px-8 py-4 text-xl border-2 border-green-500 text-white hover:bg-green-500/20 transition-all duration-300 rounded"
        >
          Enter The Matrix ⚡
        </button>
      </div>
    </header>
  );
};

export default Header;
